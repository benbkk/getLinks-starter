import React from 'react';
import Navigation from 'components/Navigation';

export default () => (
    <header className="navbar navbar-expand fixed-top text-center">
        <h1 className="text-center">I'm the frikkin Bootstrap</h1>
        <Navigation />
    </header>
);
